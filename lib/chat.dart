import 'package:flutter/cupertino.dart';

import 'div.dart';
import 'editor_list.dart';



/*
class ChatList extends StatefulWidget {
  final ChatEditController? controller;
  ChatList({this.controller, super.key});

  @override
  State<ChatList> createState() => _ChatListState();
}

class _ChatListState extends State<ChatList> {
  late ChatEditController controller;
  ListController listController = ListController();
  ChatEditController inputController = ChatEditController();

  @override
  void initState() {
    super.initState();
    controller = widget.controller ?? ChatEditController();
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        EditorListView(controller: listController),
        ChatInput(controller: inputController)
      ],
    );
  }
}*/

